query HomePage($draftId: Int, $canonicalId: QueryArgument, $provisionalDrafts: Boolean) {
    entry(section: "homePage", draftId: $draftId, draftOf: $canonicalId, provisionalDrafts: $provisionalDrafts, status: null) {
        ... on home_Entry {
            id
            title
            brow
            subtitle
            heroImage {
                url(handle: "hero")
                alt
                width(handle: "hero")
                height(handle: "hero")
            }
            buttonGroup {
                ... on button_Entry {
                    title
                    variant
                    path {
                        url
                        target
                    }
                }
            }
        }
    }
}
